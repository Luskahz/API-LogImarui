
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Base031140 {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes   
  uploader     String?
}

model Base030530 {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model Base030237 {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model Base030237Itens {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model Base030224 {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model BaseBees {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model BaseWMS {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model BaseClientes {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}

model BaseProdutos {
  id           Int      @id @default(autoincrement())
  filename     String
  uploadedAt   DateTime @default(now())
  fileBuffer   Bytes
  uploader     String?
}


model CsvBufferBases {
  id            Int      @id @default(autoincrement())
  baseId        String   // Ex: '031140'
  uploadedAt    DateTime @default(now())
  baseMinDate   DateTime // menor Dt Entrega do CSV
  baseMaxDate   DateTime // maior Dt Entrega do CSV
  fileBuffer    Bytes // Buffer do CSV
  uploader      String?
} 